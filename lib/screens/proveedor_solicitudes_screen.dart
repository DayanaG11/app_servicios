import 'package:flutter/material.dart';

class ProveedorSolicitudesScreen extends StatelessWidget {
  const ProveedorSolicitudesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Aquí irán las solicitudes recibidas del proveedor.'),
    );
  }
}
